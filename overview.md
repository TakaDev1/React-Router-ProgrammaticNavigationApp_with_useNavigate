# 課題

## 問題

`useNavigate`を使って、ボタンをクリックしたら`/about`へ移動するアプリを作成してください。

## 条件

* `/` にHomeページを表示
* `/about` にAboutページを表示
* Homeページに「Aboutへ移動」ボタンを配置
* ボタンをクリックすると`/about`へ移動
* `Link`は使用せず、`useNavigate`を使用する